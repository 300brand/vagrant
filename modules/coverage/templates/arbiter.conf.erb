# File Managed by Puppet

# Going to use the default bind_ip of 0.0.0.0
# bind_ip   = <%= @fqdn %>,<%= @ipaddress_eth1 %>

dbpath     = /var/lib/mongo-<%= @arbiter %>
replSet    = <%= @replSet %>
nojournal  = true # Do not need a pre-allocated journal as an arbiter
port       = <%= @port %>
smallfiles = true
syslog     = true
